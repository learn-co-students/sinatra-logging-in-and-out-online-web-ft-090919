<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <%if Helpers.is_logged_in?(session) == true%>
    <h1> Welcome<%= User(params["username"]) %></h1> <br><br>
    <h1><%= User(params["balance"]) %></h1> <br><br>
    <a href="/logout">Log Out</a>
    <%else%>
    <%= You Must <a href="/">Log In</a> to View Your Balance %>
    <%end%>
  </body>
</html>
